# Playwright Automation Framework (Python + Pytest-BDD)

## Features
- Playwright (Python)
- Pytest-BDD (Cucumber style)
- Allure Reporting
- Environment-based configuration (dev/qa/prod)
- CI/CD with GitHub Actions & Jenkins

## Commands
```bash
pytest --env=qa
allure serve reports/allure-results
```
